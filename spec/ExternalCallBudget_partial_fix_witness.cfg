\* A deliberate witness for why this had to be fixed at all six sites, not
\* just the one the incident report pointed at directly (runLeader's own
\* startup sequence): even with that pair fixed, leaving any other site
\* unbounded still permanently strands reconciliation - the property is
\* about the whole set, not any single member of it.
\* Expect: NeverPermanentlyStuck violated via the one site left out.
SPECIFICATION Spec
CONSTANTS
  BoundedCallSites = {"ScaleSetLookup", "SessionEstablish", "AWSPriceRefresh",
                       "AzurePriceRefresh", "GCPPriceRefresh"}
INVARIANT TypeOK
INVARIANT NeverPermanentlyStuck
