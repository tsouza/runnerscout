\* All four known sites patched (#168), PLUS a fifth, deliberately-unpatched
\* leak path (a stand-in for "a future bug, or GitHub diverging for any
\* other reason" -- event E31), PLUS today's REAL #161 pruning design
\* (gated on terminal phase + age alone, never on registration state).
\* Tests whether patching the four known sites is actually sufficient.
\* Expect: NoIrrecoverableOrphan is violated.
SPECIFICATION Spec
CONSTANTS
  AllocIDs = {a1, a2}
  DeregOnPendingTimedOut = TRUE
  DeregOnCreatingDeleted = TRUE
  DeregOnRunningDeleted = TRUE
  DeregOnDeletingDeleted = TRUE
  EnableUnknownLeak = TRUE
  EnableReconciliation = FALSE
  PruneRequiresChecked = FALSE
INVARIANT TypeOK
INVARIANT NoIrrecoverableOrphan
