\* Pre-#179 shape: every allocation may issue a JIT request immediately,
\* so a batch can start two requests in the same clock slot.
\* Expect: NoJitBurst violated.
SPECIFICATION Spec
CONSTANTS
  AllocIDs = {a1, a2}
  MaxClock = 2
  SpacingEnabled = FALSE
INVARIANT TypeOK
INVARIANT NoJitBurst
