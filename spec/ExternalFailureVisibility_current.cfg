\* The current code after #178: JIT failures preserve their cause, but a
\* cloud-create failure still records only CreateCommitmentUnknown.
\* Expect: FailureCauseVisible violated (the cloud-create gap remains).
SPECIFICATION Spec
CONSTANTS
  AllocIDs = {a1, a2}
  PreserveJITCause = TRUE
  PreserveCloudCreateCause = FALSE
INVARIANT TypeOK
INVARIANT FailureCauseVisible
