\* The listener's message-session protocol as it exists today - no config
\* variants, because there is no alternate design shipped yet to compare
\* against (unlike TickConcurrency's buggy/fixed pair). This is a disclosed,
\* unfixed limitation, not a patched historical bug.
\* Expect: NoSilentlyStrandedDemand violated - SessionGoesStale then
\* JobBecomesAvailable reaches a state with real demand pending and no
\* internal action able to acquire it, matching the production incident.
SPECIFICATION Spec
INVARIANT TypeOK
INVARIANT NoSilentlyStrandedDemand
