\* Reproduces the issue #176 diagnosability gap: a JIT failure is collapsed
\* to the generic CreatePreparationFailed sentinel.
\* Expect: FailureCauseVisible violated.
SPECIFICATION Spec
CONSTANTS
  AllocIDs = {a1, a2}
  PreserveJITCause = FALSE
  PreserveCloudCause = TRUE
INVARIANT TypeOK
INVARIANT FailureCauseVisible
