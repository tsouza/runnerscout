\* Reproduces the issue #176 diagnosability gap before #178: a JIT failure
\* is collapsed to CreatePreparationFailed; other failure classes are kept
\* preserved so the named violation is specifically the JIT swallow.
\* Expect: FailureCauseVisible violated.
SPECIFICATION Spec
CONSTANTS
  AllocIDs = {a1, a2}
  MaxAttempts = 2
  EnableTimeout = TRUE
  PreserveJITCause = FALSE
  PreserveWireGuardCause = TRUE
  PreserveCloudCreateCause = TRUE
  PreserveObserveCause = TRUE
  PreserveDeleteCause = TRUE
INVARIANT TypeOK
INVARIANT FailureCauseVisible
