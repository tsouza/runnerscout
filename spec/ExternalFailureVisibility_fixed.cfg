\* The fixed shape: both external failure classes preserve their own cause.
\* Expect: clean.
SPECIFICATION Spec
CONSTANTS
  AllocIDs = {a1, a2}
  PreserveJITCause = TRUE
  PreserveCloudCause = TRUE
INVARIANT TypeOK
INVARIANT FailureCauseVisible
