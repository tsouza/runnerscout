\* PR #168 (all four known sites deregister) plus the current shipped
\* re-verifying prune design, no unknown leak. Both invariants clean.
SPECIFICATION Spec
CONSTANTS
  AllocIDs = {a1, a2}
  DeregOnPendingTimedOut = TRUE
  DeregOnCreatingDeleted = TRUE
  DeregOnRunningDeleted = TRUE
  DeregOnDeletingDeleted = TRUE
  EnableUnknownLeak = FALSE
  PruneReVerifies = TRUE
INVARIANT TypeOK
INVARIANT NoOrphanedRegistration
INVARIANT NoIrrecoverableOrphan
