\* The shipped fix: every known call site now has its own externalCallBudget
\* (or, for the two runLeader steps, githubStartupBudget) context,
\* independent of the bare cancel-only ctx above it. See CallSites's own
\* comment for why "every known" is deliberately not claimed as "every".
\* Expect: clean - a stall at any modeled site now recovers rather than
\* sticking.
SPECIFICATION Spec
CONSTANTS
  BoundedCallSites = {"ScaleSetLookup", "SessionEstablish", "PruneDeregister",
                       "AWSPriceRefresh", "AzurePriceRefresh", "GCPPriceRefresh",
                       "AzureInterruptionPoll", "RetryAttemptJobs", "RetryRerunFailedJobs"}
INVARIANT TypeOK
INVARIANT NeverPermanentlyStuck
