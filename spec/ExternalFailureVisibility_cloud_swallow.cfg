\* The extrapolated sibling: a real cloud-create failure is collapsed to
\* the same generic sentinel instead of CloudCreateFailure.
\* Expect: FailureCauseVisible violated.
SPECIFICATION Spec
CONSTANTS
  AllocIDs = {a1, a2}
  PreserveJITCause = TRUE
  PreserveCloudCause = FALSE
INVARIANT TypeOK
INVARIANT FailureCauseVisible
