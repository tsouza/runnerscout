\* Before this PR: none of the six call sites had their own timeout -
\* every one used the bare ctx from runLeader (cancel-only, no deadline).
\* Expect: NeverPermanentlyStuck violated - any one of the six stalling
\* reaches a permanently stuck state, matching the production incident
\* exactly (a leader pod hung for 49+ minutes, idle CPU, no log line).
SPECIFICATION Spec
CONSTANTS
  BoundedCallSites = {}
INVARIANT TypeOK
INVARIANT NeverPermanentlyStuck
