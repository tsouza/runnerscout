\* recovery.go's own p.MaxRetries < 1 || p.MaxRetries > 3 bounds the real
\* domain to exactly {1, 2, 3} - three cheap configs cover it completely
\* rather than picking one arbitrary value.
SPECIFICATION Spec
CONSTANTS
  MaxRetries = 1
INVARIANT TypeOK
INVARIANT RetryBoundRespected
