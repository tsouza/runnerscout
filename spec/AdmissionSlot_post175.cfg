\* The shipped fix (PR #175): ReleaseSlot's guard covers both terminal
\* phases, matching the full set EveryTerminalIsReleasable checks against.
\* Expect: clean.
SPECIFICATION Spec
CONSTANTS
  AllocIDs = {a1, a2}
  MaxAdmitted = 2
  ReleasablePhases = {"Deleted", "TimedOut"}
INVARIANT TypeOK
INVARIANT EveryTerminalIsReleasable
