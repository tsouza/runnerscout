---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: runnerscalesets.runnerscout.io
spec:
  group: runnerscout.io
  names:
    kind: RunnerScaleSet
    listKind: RunnerScaleSetList
    plural: runnerscalesets
    singular: runnerscaleset
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              budgetRef:
                description: |-
                  BudgetRef bounds worst-case daily spend for this scale set. A
                  CapacityBudget referenced by more than one RunnerScaleSet is not a
                  shared pool: each referencing scale set enforces the same ceiling
                  independently against only its own admitted allocations.
                properties:
                  name:
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              github:
                properties:
                  auth:
                    properties:
                      appClientID:
                        type: string
                      appInstallationID:
                        format: int64
                        minimum: 1
                        type: integer
                      mode:
                        enum:
                        - pat
                        - app
                        type: string
                      secretRef:
                        properties:
                          key:
                            maxLength: 253
                            minLength: 1
                            pattern: ^[-._a-zA-Z0-9]+$
                            type: string
                          name:
                            maxLength: 253
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9.]*[a-z0-9])?$
                            type: string
                        required:
                        - key
                        - name
                        type: object
                    required:
                    - mode
                    - secretRef
                    type: object
                    x-kubernetes-validations:
                    - message: App authentication requires client and installation
                        identity
                      rule: self.mode != 'app' || (has(self.appClientID) && size(self.appClientID)
                        > 0 && has(self.appInstallationID))
                    - message: PAT authentication excludes App settings
                      rule: self.mode != 'pat' || (!has(self.appClientID) && !has(self.appInstallationID))
                  scaleSetID:
                    minimum: 1
                    type: integer
                  url:
                    type: string
                required:
                - auth
                - scaleSetID
                - url
                type: object
              maxLifetimeSeconds:
                maximum: 21600
                minimum: 1
                type: integer
              maxRunners:
                maximum: 10
                minimum: 1
                type: integer
              provisioningSeconds:
                maximum: 600
                minimum: 1
                type: integer
              runnerClassRef:
                description: |-
                  LocalReference cannot name another namespace. Controllers must resolve it in
                  the namespace of the referring resource.
                properties:
                  name:
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - name
                type: object
              suspend:
                type: boolean
            required:
            - github
            - maxLifetimeSeconds
            - maxRunners
            - provisioningSeconds
            - runnerClassRef
            type: object
            x-kubernetes-validations:
            - message: lifetime must cover provisioning deadline
              rule: self.maxLifetimeSeconds >= self.provisioningSeconds
          status:
            description: ConfigurationStatus contains references and redacted conditions,
              never credentials.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
